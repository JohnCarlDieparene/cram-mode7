<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.labactivity.crammode">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Cram Mode"
        android:roundIcon="@drawable/white_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CramMode"
        tools:replace="android:fullBackupContent"
        tools:targetApi="31">



        <!-- Splash Screen (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.CramMode.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>



        <!-- App Activities -->
        <activity android:name=".AuthComposeActivity" android:exported="true"/>
        <activity android:name=".HomeComposeActivity" android:exported="true"/>
        <activity android:name=".QuizHistoryViewerActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".DisplaySummariesActivity" />
        <activity android:name=".FlashcardViewerActivity" />
        <activity android:name=".FullscreenImageActivity" />
        <activity android:name=".HistoryActivity"/>
        <activity android:name=".AboutActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".PrivacyPolicyActivity" />
        <activity android:name=".SummaryActivity" />
        <activity android:name=".FlashcardsActivity" />
        <activity android:name=".QuizViewerActivity" />
        <activity android:name=".SettingsActivity" />








        <!-- Fonts -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <!-- FileProvider for camera and gallery access -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.labactivity.crammode.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
